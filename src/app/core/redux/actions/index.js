import { actions as user } from '~/features/login';
import * as navigation from './navigation';
import * as routing from './routing';
import * as version from './version';

export default { navigation, routing, user, version };
